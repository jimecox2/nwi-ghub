export const metadata = {
  title: "Servers — Northern Wireless",
  description: "Pre-configured DNS, DHCP, HTTP and FTP servers.",
};

# Servers

Pre-configured servers for providing all your networking needs — such as DNS,
DHCP, HTTP, and FTP — are supplied by Northern Wireless.
